{
  "name": "qr-attendance-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}

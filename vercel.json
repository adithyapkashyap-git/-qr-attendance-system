{
  "version": 2,
  "buildCommand": "cd frontend && npm install react-scripts && npm install && npm ls && npm run build",
  "installCommand": "cd frontend && npm install react-scripts && npm install",
  "outputDirectory": "frontend/build"
}
